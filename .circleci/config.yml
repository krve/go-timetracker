version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9


    working_directory: /go/src/github.com/krve/go-timetracker
    steps:
      - checkout
      - run: sudo apt install libappindicator3-dev gir1.2-appindicator3-0.1

      - run: go get -v -t -d ./...
      - run: go test -v ./...